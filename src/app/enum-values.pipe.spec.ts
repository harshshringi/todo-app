import { EnumValuesPipe } from './enum-values.pipe';

describe('EnumValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
